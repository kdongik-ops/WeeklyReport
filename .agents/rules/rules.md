---
trigger: always_on
---

# 주간보고 업무 워크플로우

주간보고 작성 시 다음 프로세스를 준수합니다.

## 공통 원칙
- **읽기 전용 명령 자동 실행**: `npm list`, `dir`, `type` 등 시스템 상태를 읽기만 하는 명령은 사용자 승인 없이 `SafeToAutoRun: true`로 설정하여 자동으로 진행합니다.

---

## 1단계: 초안 생성
1. **소스**: `raw_data` 폴더에서 해당 주차 텍스트 읽기.
2. **초안 생성**: `agent.md`의 요약/그룹화 원칙을 적용하여 텍스트(.txt) 파일 생성. (주의: 사용자가 축약된 내용의 출처를 파악할 수 있도록, 초안 작성 시 **각 항목별로 원본 데이터(RAW)와 요약된 결과(요약)를 함께 비교할 수 있는 형식**으로 보여주어야 함)
3. **파일명**: `output/[주차]_초안.txt`
4. **검토 요청**: 사용자에게 내용 검토(오타, 누락 등) 요청.

## 2단계: 최종본 생성
1. **트리거**: 사용자의 "이상 없음" 또는 "최종본 생성" 요청 시.
2. **생성**: 초안에 `agent.md`의 위키 디자인 규칙(HTML, 볼드 등) 적용. 단, 최종본 생성 시에는 초안의 **`[RAW]` 내용은 제외하고 `[요약]` 부분만을 취합**하여 생성한다.
3. **파일명**: `output/[주차].html`

---

## 마무리
1. `output/[주차].html` 파일 브라우저로 열기.
2. 텍스트 복사(`Ctrl+A`, `Ctrl+C`).
3. 사내 위키에 붙여넣기(`Ctrl+V`).